{
  "name": "meatlytics",
  "version": "0.1.0",
  "description": "Embeddable, self-hosted website analytics as Express middleware",
  "main": "src/index.js",
  "files": ["src", "dist", "scripts", "README.md"],
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.js",
    "prepare": "node scripts/build.js"
  },
  "keywords": ["analytics", "privacy", "self-hosted", "express", "middleware", "sqlite"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GraphicMeat/meatlytics.git"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "license": "AGPL-3.0-only"
}
